language: c
sudo: required

os:
    - linux


compiler:
  #- gcc
  - gcc-7
  #- clang
addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test # gcc
        packages:
            - gcc-7
            - make
            - cmake
            - cmake-data


before_install:
      - echo uname -a
      - echo gcc -v
      - make cmock-install



script:
  - make
  # tests
  - make testall
  - make clean
  - make distclean